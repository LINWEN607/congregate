
def bitbucket_and_gitlab_names_match(bitbucket_name, gitlab_name):
    """
    Function that checks if a bitbuckeet name and a gitlab name match for a repo (GitLab project). It does this by
    replacing the characters that are typically created
    :param bitbucket_name:
    :param gitlab_name:
    :return:
    """